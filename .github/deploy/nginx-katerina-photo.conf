server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;
    root /var/www/katerina-photo;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~ /\. {
        deny all;
    }
}
